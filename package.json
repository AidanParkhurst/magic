{
  "name": "magic",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "devhost": "vite --host",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "npm run build && gh-pages -d dist",
    "deploy:clean": "npm run build && gh-pages -d dist --no-history"
  },
  "devDependencies": {
    "@sveltejs/vite-plugin-svelte": "^6.2.1",
    "gh-pages": "^6.1.1",
    "svelte": "^5.43.8",
    "vite": "^7.2.4"
  },
  "dependencies": {
    "@emailjs/browser": "^4.4.1",
    "emailjs": "^5.0.0",
    "svelte-routing": "^2.13.0",
    "svelte-spa-router": "^4.0.1"
  },
  "homepage": "https://funmagic.live"
}
